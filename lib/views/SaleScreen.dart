import 'package:flutter/material.dart';

class Sales_Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        child:Center(
          child: Text('Ventes'),
    )
    );
  }
}
